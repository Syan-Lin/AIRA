调整下交互界面，用户输入aira之后，先打开QWEN的CLI界面，然后采用拟态窗口或已有的选项方式，引导用户选择。
当用户在当前界面需要切换交互模式，通过快捷键或/change的命令，来切换，同时清空上下文（需要有提示）。
同时涉及到的交互模式，需要融入到qwencode的cli中。

## 记忆具体实现

通过 hook，在 session 开始时注入 index（时间+一句话描述），提示可以按需求 .qwen/memory 中获取对应的记忆

在 session end 时，将对话原文保存到 memory 中（标记为未处理）

添加 dream skill，对原文 memory 进行摘要（标记为处理），同时建立关联（包括记忆和具体的知识），类比知识库
